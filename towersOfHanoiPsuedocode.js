
class TowersOfHanoi {
    run() {
        // until towers[0] === [] && (towers[1] || towers[2]) === [3, 2, 1]
            // ask the player for a move
            // check if move is valid
            // execute move
    }
}